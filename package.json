{
  "name": "california-fires",
  "version": "1.0.0",
  "description": "Alexa skill to detect fires close to you",
  "scripts": {
    "build": "node_modules/.bin/webpack-cli",
    "deploy-lambda": "npm run build && ask deploy -t lambda",
    "deploy": "npm run build && ask deploy"
  },
  "author": "whirish <lpoflynn@protonmail.ch>",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "eslint": "^5.9.0",
    "eslint-config-airbnb-base": "^13.1.0",
    "eslint-config-prettier": "^3.3.0",
    "eslint-plugin-import": "^2.14.0",
    "eslint-plugin-prettier": "^3.0.0",
    "prettier": "^1.15.2"
  },
  "dependencies": {
    "webpack": "^4.26.0",
    "webpack-cli": "^3.1.2"
  }
}
